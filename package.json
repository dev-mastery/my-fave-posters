{
  "name": "my-fave-posters",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "normalize.css": "^8.0.1",
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "react-scripts": "2.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postbuild": "NODE_ENV=build npm run test:ui",
    "test": "react-scripts test --env=jsdom",
    "test:ui": "jest -c jest.config.js",
    "test:ui:watch": "npm run test:ui -- --watch",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app",
    "globals": {
      "page": "readonly",
      "browser": "readonly",
      "jestPuppeteer": "readonly"
    }
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@babel/polyfill": "^7.2.5",
    "expect-puppeteer": "^4.0.0",
    "jest-puppeteer": "^4.0.0",
    "puppeteer": "^1.12.2",
    "serve": "^11.0.2"
  }
}
